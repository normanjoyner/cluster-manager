package handlers

import (
	"testing"
)

func TestGetNodes(t *testing.T) {
	// nodes := getNodes()
	//
	// r := reflect.TypeOf(nodes);
	//
	// if  r.String() != "node" {
	//     t.Errorf("Expected getNodes() to be of type nodes, but was of %v", r)
	// }
}
